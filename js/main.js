$(document).ready(function() {


/*
	$('something').mouseenter(function() {
		// action when hovering
	}).mouseleave(function() {
		// action when hovering ceases
	});
*/

/*
	setTimeout(function(){
		// action after 900ms
	}, 900);
*/

// window.location = 'page I want to navigate to.html';

/*
	$(window).scroll(function() {
		var scrollTop = $(window).scrollTop();
		// do something based on scrollTop
	});
*/

/*
	$(window).load(function() {
		// action to occur right when page loads
	});
*/

/*
	$.fn.scrollView = function () {
	  return this.each(function () {
	    $('html, body').animate({
	      scrollTop: $(this).offset().top
	    }, 1000); // will take 1sec to scroll here
	  });
	}

	$('#element-I-want-to-scroll-to').scrollView();
*/

});